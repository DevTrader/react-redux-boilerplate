import * as actionTypes from '../Actions/action-list-handler';

const initialState = {
    listArr: ['item 1', 'item 2', 'item 3', 'item 4', 'item 5', 'item 6']
}

const reducer = (state = initialState, action) => {

};

export default reducer;